# ⚠️ SECURITY WARNING ⚠️
# This is a TEMPLATE file. Copy it to .env and fill in your actual values.
# NEVER commit the .env file to Git - it contains sensitive credentials!

# ============================================
# RELAYER SECRET KEY (CRITICAL)
# ============================================
# Generate a NEW keypair: solana-keygen new --outfile relayer-keypair.json
# Extract base58 secret: solana-keygen pubkey relayer-keypair.json (shows public key)
# Use the secret array from the JSON file
RELAYER_SECRET_KEY=your_base58_secret_key_here

# ============================================
# SOLANA CONFIGURATION
# ============================================
# RPC endpoint (devnet for testing, mainnet for production)
SOLANA_RPC_URL=https://api.devnet.solana.com

# Deployed Z-Cresca program ID
PROGRAM_ID=HX5nu29URyVfVFDsiVRumvf64egFydNqgGibBpPxt3nz

# ============================================
# EXTERNAL SERVICES
# ============================================
# Jupiter Aggregator API for token swaps
JUPITER_API_URL=https://quote-api.jup.ag/v6

# ESP32 Hardware Entropy Source (for Privacy Cash)
ESP32_ENTROPY_URL=http://your-esp32-ip/entropy

# ============================================
# RELAYER SERVER SETTINGS
# ============================================
RELAYER_HOST=0.0.0.0
RELAYER_PORT=8080

# ============================================
# PRIVACY FEATURES
# ============================================
PRIVACY_CASH_ENABLED=true

# Use Privacy Cash SDK (requires Node.js service running)
USE_PRIVACY_CASH_SDK=true

# Privacy Cash Service URL (Node.js bridge)
PRIVACY_CASH_SERVICE_URL=http://127.0.0.1:8081
PRIVACY_CASH_SERVICE_PORT=8081

# Path to relayer keypair for Privacy Cash SDK
RELAYER_KEYPAIR_PATH=./relayer-keypair.json

# ============================================
# OPTIONAL: PRODUCTION CONFIGURATION
# ============================================
# DATABASE_URL=postgresql://user:pass@localhost/z_cresca
# REDIS_URL=redis://localhost:6379
